# Lackofleetiosis

#### Dev setup

* Run `bin/setup`
* Fill in `.zenv.rb`
* Run `yarn --cwd ui`

#### Start app

* run `rails s`
* In `ui` dir, run `ng serve`
* Visit http://localhost:4200/

#### Run tests

* Run `rspec`

### CLI

* Run `bin/app` to see available commands
